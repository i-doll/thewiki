[package]
name = "thewiki-storage"
description = "Storage abstractions for thewiki (sqlx + object_store)."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
thewiki-core = { workspace = true }
# sqlx is intentionally NOT a runtime dependency at this point — this crate is
# a shell pending #6, which lands the SQLite adapter and will add sqlx with
# only the features that adapter actually uses. The dev-dependency below is
# what the migration integration test needs.

[dev-dependencies]
sqlx = { workspace = true }
tokio = { workspace = true }

[lints]
workspace = true
