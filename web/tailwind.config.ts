import type { Config } from "tailwindcss";

// Tailwind v4 is CSS-driven (via `@import "tailwindcss"` and `@theme` blocks
// in `src/styles.css`). Content scanning is automatic in v4 — no `content`
// array required. This file exists as an extension point for future
// JS-driven customisation (custom plugins, runtime-computed theme tokens).
export default {} satisfies Config;
